# CLAUDE.md

Google Apps Script (GAS) プロジェクトの作業メモ。Claude Code はセッション開始時にこのファイルを自動で読む。

## このファイルの育て方（Claudeへの指示）

- 作業がひと区切りついたら、そのたびに「確定した仕様・決めたこと」「つまずいた点・原因」
  「残作業・次にやること」をこのファイルに追記すること。書きっぱなしにせず、
  内容が古くなったら書き換える（削除してよい情報は削除する）。
- ユーザーから「CLAUDE.mdを更新して」と言われたら、その時点までの会話・変更内容を
  反映して全体を見直すこと（差分の下書きだけでなく、矛盾した記述の整理も行う）。
- 推測や一般論で書かない。実際にコード（`Code.gs` / `Index.html`）と `notes.md` を
  読んで確認した内容を書くこと。
- 個人情報・APIキー・スプレッドシートの内容そのものなど、公開リポジトリに書くべきでない
  情報は書かない（このリポジトリは public）。スクリプトIDやスプレッドシートIDは
  既に `notes.md` / `.clasp.json` に記載されており秘匿情報ではないため、参照は可。

## このプロジェクトは何か

「If-Then：地球防衛オペレーション」— ユーザー（大輝さん）の三日坊主を防ぐための
習慣化支援 Web アプリ。Google Apps Script（GAS）のスタンドアロン Web アプリとして
動作し、Gemini API でキャラクターとストーリーを毎日生成することで、単調な
「継続チェック」を飽きさせない演出に変えている。

具体的な仕組み:
- ユーザーは「継続したいこと（例：毎日スクワット20回）」と通知したい時刻を入力して
  ミッションを1つ作成する（`createMission`）。作成時に Gemini がキャラクター名・
  口調・世界観・If-Thenトリガー（「〇〇したら→△△する」形式）・ミッション文・
  達成/未達成時のリアクション文を一括生成し、`missions` シートに固定保存する。
  ミッション自体・If-Thenトリガー・世界観は以後変わらない。
- **毎日アプリを開くたびに新しいキャラクターが登場する**（`getTodayMission`）。
  同じミッションでも日によって全く違うキャラが指令を届ける。10回に1回は
  「特別通信官」という称号付きの間欠強化パターンになる。
- 「任務完了」（達成）・「エネルギー不足＝タイムリープ特異点」（未達成）の
  ボタンを押すと、そのキャラの反応セリフが表示される。このセリフは
  **その日のキャラ生成時に Gemini へ1回だけ問い合わせて生成・事前保存済み**
  （`doneResponse` / `skipResponse`）であり、報告時 (`report_`) には API を
  呼ばない。旧データ等でこの事前生成分が無い場合のみ、フォールバックとして
  その場で Gemini を呼ぶ。
- 未達成時は「1回だけやってみる（タイムリープ完遂）」という最小達成ハードルの
  ボタンが出て、そちらを選ぶと `reportDone` 扱いになる（サボりを完全な失敗に
  しないための工夫）。
- 統計画面ではストリーク（連続達成日数、現在・過去最高）、直近60日のカレンダー、
  ミッションごとのアーカイブ（日々のキャラのやり取りログ）を確認できる。
- `checkAndNotify`（1時間おきの時間主導トリガー）が、その時間帯が通知時刻の
  ミッションで当日まだ記録がないものについてメール通知を送る。

心理学的な設計意図（`notes.md` より）:
- If-Thenプランニング／損失回避（具体的でシュールな「破滅」描写）／
  目標の最小化（「1回だけ」の救済ボタン）／66日進捗（習慣化の目安）／
  間欠強化（10回に1回の特別演出）を組み込んでいる。
- キャラクターは「ロボット・宇宙人・AI・魔法使い」といった既存SFテンプレートを
  避け、まったく異なる概念を掛け合わせたシュールな新キャラを毎回生成し、
  口調・語尾を最初から最後まで崩さないこと、実在の日本の固有名詞を最低1つ
  入れることをプロンプトで強制している（`Code.gs` の `CHAR_RULES_` 定数）。

## 主要ファイル・技術スタック

- **Google Apps Script（V8ランタイム）**+ **clasp** によるローカル開発・デプロイ。
  `.clasp.json` の `scriptId` が紐づくGASプロジェクト本体。
- `Code.gs` — バックエンド全体（473行）。主な関数:
  - `doGet()` — Webアプリのエントリーポイント。`Index.html` を返す。
  - `callGemini_(systemPrompt, userMessage)` — Gemini API
    (`gemini-2.5-flash`, `temperature: 1.2`) を呼ぶ共通ヘルパー。
    APIキーはハードコードではなく `PropertiesService.getScriptProperties()`
    から取得（スクリプトプロパティに保存、リポジトリには含まれない）。
  - `parseJson_` — Geminiの応答からコードフェンス（```json）を除去してパース。
  - `CHAR_RULES_` — キャラ生成の共通ルール定数（`generateMissionOptions`と
    `getTodayMission`のプロンプトで共有）。
  - `getMissionsSheet_` / `getDailyLogsSheet_` — シートが無ければヘッダー行込みで
    自動作成する。列レイアウトはコード内コメントに記載（下記参照）。
  - `ensureNotifyTrigger_` / `checkAndNotify` — 1時間おきの時間主導トリガーで
    通知メールを送る。
  - `generateMissionOptions` → `saveMission`（`createMission`がまとめて呼ぶ）—
    ミッション新規作成時にキャラ・世界観・初日ログを一括生成・保存。
  - `getTodayMission` — 当日分のキャラ生成（未生成なら1日1回だけAPI呼び出し、
    生成済みならキャッシュ／シートから返す）。
  - `reportDone` / `reportSkip`（内部は共通の `report_`）— 事前生成済みの
    `doneResponse`/`skipResponse` をシートに反映して返す。無ければフォールバックで
    Geminiを呼ぶ。
  - `getStats` — ストリーク・カレンダー・累計日数の集計。
  - `getArchive` / `deleteMission`（実体は `status` を `archived` にするだけの
    論理削除）。
  - `CacheService.getScriptCache()` を使い、`missions` / `today_*` / `stats_*`
    をキー化して5分キャッシュ（`cacheGet_` / `cachePut_` / `cacheRemove_`）。
- `Index.html` — フロントエンド一式（591行、HTML+CSS+JSを1ファイルに同梱）。
  `google.script.run` でGAS側の関数を呼び出すシンプルな構成。タブは
  「作戦本部」（ミッション一覧・当日のキャラとのやり取り）と「戦績」
  （カレンダー・アーカイブ）の2つ。白基調UI（背景 `#F3F7FF`、アクセント
  `#1A5CCC`、サブアクセント `#0088BB`）、スマホ最適化（16px以上フォント、
  `env(safe-area-inset-bottom)` 対応）。
- `appsscript.json` — マニフェスト。タイムゾーン `Asia/Tokyo`、Webアプリの
  実行ユーザーは `USER_DEPLOYING`、アクセス範囲は `MYSELF`（本人専用アプリ）。
- `.clasp.json` — clasp の紐付け設定（`scriptId` と `rootDir`）。
- `deploy.sh` — デプロイ用スクリプト（下記参照）。
- `notes.md` — 開発メモ。仕様の一次情報源として必ず参照すること。

### スプレッドシートの列レイアウト（`Code.gs` 内コメントより）

`missions` シート:

| 列 | 内容 |
|---|---|
| 0 | id |
| 1 | goalText |
| 2 | ifThenTrigger |
| 3 | worldStory |
| 4 | createdDate |
| 5 | status（`active` / `archived`） |
| 6 | timing（表示用の日本語文字列） |
| 7 | worldSetting |
| 8 | notifyTime（`HH:mm`） |

`dailyLogs` シート:

| 列 | 内容 |
|---|---|
| 0 | id |
| 1 | missionId |
| 2 | date |
| 3 | characterName |
| 4 | characterPersonality |
| 5 | result（`''` / `done` / `skip`） |
| 6 | responseText（キャラの自己紹介 or 今日の指令） |
| 7 | doneResponse（事前生成済み） |
| 8 | skipResponse（事前生成済み） |

スプレッドシートID・GASスクリプトID・スクリプトプロパティのキー名は
`notes.md` に記載がある（値自体はスクリプトプロパティ側にあり本リポジトリには
含まれない）。

## 開発の進め方・デプロイ方法

1. ローカルで `Code.gs` / `Index.html` を編集する。
2. `./deploy.sh` を実行する。中身は:
   - `clasp push --force` — ローカルの変更をGASプロジェクトへ反映。
   - `git add -A && git commit` → `git push origin main` — GitHubへも反映
     （コミットメッセージは自動生成の日時のみ。実質的な変更内容は別途
     コミットメッセージやこのファイルに残すことを検討する）。
3. **重要**: `clasp push` しただけでは公開中のWebアプリ（HEAD deployment）には
   反映されない場合がある。`deploy.sh` のコメントには「HEAD deploymentが
   自動更新される」とあるが、反映されない/古い挙動のままに見える場合は
   GASエディタで「デプロイを管理」→ 対象のデプロイを編集 → バージョンを
   「新バージョン」にして再デプロイすることを確認する。
4. Webアプリの実行設定は `appsscript.json` により本人専用
   （`access: MYSELF`）。他ユーザーへの共有は想定されていない。
5. スクリプトプロパティ（GASエディタの「プロジェクトの設定」→
   「スクリプト プロパティ」）に以下が必要:
   - `GEMINI_API_KEY`
   - `SPREADSHEET_ID`

## 気づいた注意点・ハマりどころ

- Gemini呼び出しは「ミッション作成時」と「その日初回のキャラ生成時」の
  1日1往復（＋任意でフォールバック時）に絞られており、`report_` では
  基本的にAPIを呼ばない設計になっている。挙動を変える際はこの
  「1日1回だけ呼ぶ」制約を壊さないよう注意する（コスト・レイテンシ対策として
  意図的な設計）。
- `getTodayMission` の「特別通信官」判定は `totalLogs % 10 === 0`
  （`dailyLogs` の行数、全ミッション共通のカウント）で行っている。
  ミッション単位のカウントではない点に注意。
- `checkAndNotify` はミッションの `notifyTime` の「時」だけを見て1時間単位で
  トリガーしており、分単位の精度はない（トリガー自体が `everyHours(1)`）。
- キャラのプロンプト（`CHAR_RULES_` および各生成関数内のルール）は非常に
  細かく作り込まれている。トーンや口調のブレを直す場合は、まずこの
  プロンプト文面を読んでから編集すること（ハードコードされた例文が
  Geminiの出力スタイルに強く影響している）。
- git の履歴は現状1コミットのみ（`deploy.sh` 経由の自動コミットメッセージが
  日時だけになりがちなので、変更内容を追うには実際に diff を見る必要がある）。
- 本リポジトリは public。スプレッドシートIDやスクリプトIDはコード上
  秘匿情報として扱われていないが、Gemini APIキーやユーザー個人のメール本文・
  ミッション内容などをコード側にハードコードしないこと。
